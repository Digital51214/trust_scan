<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ Internet (APIs) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ✅ Camera (Camera option in bottom sheet) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ✅ Android 13+ (API 33+) gallery images permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- ✅ Android 12 and below gallery permission -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:label="Trust Scan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- ✅ Existing: Text processing (OCR etc.) -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- ✅ Rate Us: Play Store open karne ke liye -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!-- ✅ Rate Us: market:// scheme (direct Play Store app) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>

        <!-- ✅ Contact Us: Email app open karne ke liye -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>

        <!-- ✅ Share App: share_plus ke liye -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>

    </queries>

</manifest>